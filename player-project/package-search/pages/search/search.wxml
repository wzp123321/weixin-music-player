<view class="search">
  <van-search value="{{ searchLabel }}" placeholder="请输入搜索关键词" show-action bind:search="handleSearch" bind:change="handleChange" />
  <!-- 初始进入 -->
  <search-initial wx:if="{{!searchLabel && !searchFlag}}"></search-initial>

</view>